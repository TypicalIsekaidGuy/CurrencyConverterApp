package com.example.currencyconverterapp

import androidx.compose.runtime.MutableState

/*
object NumPad: NumPadInterface {
    override fun inputNumber(int: Int,cursorPosition: Int,  changeValue: MutableState<Float>, otherValue:) {
        changeValue.value = (changeValue.value.toString().filterIndexed { index, c -> index< cursorPosition} + int.toString() + changeValue.value.toString().filterIndexed { index, c -> index>= cursorPosition}).toFloat()
        changeValues()
    }

    override fun delete() {
        TODO("Not yet implemented")
    }

    override fun putPoint() {
        TODO("Not yet implemented")
    }

    override fun changeValues(amount: MutableState<Float>, totalSum: MutableState<Float>) {
        TODO("Not yet implemented")
    }
}*/
